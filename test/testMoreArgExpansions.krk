import kuroko
let l = [x for x in range(1,1000)]

print(l)
#print(kuroko.getstackstatus())

def foo(*args):
    #print(kuroko.getstackstatus())
    print(len(args))
    print(args)
    for i in args:
        print(i)

foo(*l)
#foo(*l,*l)
#foo(*l,*l,*l)
